# Configura��o da aplica��o
server.port=8080
spring.application.name=EcoMonitor

# Banco de dados H2 (em mem�ria)
spring.datasource.url=jdbc:h2:mem:eccodb
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=123

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG

security.secret-key=unipar
security.expiration-time=300000

security.cors.origin=http://localhost:8080,http://*.localhost:8081,http://*.localhost:4200